general:
    user: !ENV ${USER}
    setup_name: "awicm3"
    version: "master"
    account: "hhb20"
    compute_time: "06:00:00"
    initial_date: "1990-01-01"
    final_date: "2040-01-01"
    base_dir: "/p/scratch/chhb20/${user}/runtime/${general.setup_name}-${general.version}/"
    nday: 0
    nmonth: 0
    nyear: 10

awicm3:
    postprocessing: false
    model_dir: "/p/project/chhb19/${user}/model_codes/${general.setup_name}-${general.version}/"
    pool_dir: "/p/project/chhb19/jstreffi/input/"

fesom:
    resolution: "CORE2"
    pool_dir: "/p/project/chhb19/jstreffi/input/fesom2/"
    mesh_dir: "/p/project/chhb19/jstreffi/input/fesom2/core2/"
    restart_rate: 10
    restart_unit: "y"
    restart_first: 10
    lresume: false
    time_step: 2400
    nproc: 432
    add_namelist_changes:
        namelist.io:
           nml_list:
                io_list: [ 'sst',1, 'm', 4, 'sss',1, 'm', 4,'ssh',1, 'm', 4,'uice',1, 'd', 4,'vice',1, 'd', 4,'a_ice',1, 'm', 4,'m_ice',1, 'm', 4,'m_snow',1, 'm', 4,'MLD1',1, 'm', 4,'MLD2',1, 'm', 4,'MLD3',1, 'm', 4,'tx_sur',1, 'm', 4,'ty_sur',1, 'm', 4,'temp',1, 'm', 4,'salt',1, 'm', 8,'N2',1, 'y', 4,'Kv',1, 'y', 4,'u',1, 'y', 4,'v',1, 'y', 4,'unod',1, 'y', 4,'vnod',1, 'y', 4,'w',1, 'y', 4,'Av',1, 'y', 4,'bolus_u',1, 'y', 4,'bolus_v',1, 'y', 4,'bolus_w',1, 'y', 4]

oifs:
    resolution: "TCO95"
    levels: "L91"
    prepifs_expid: aack
    input_expid: awi3
    lresume: false
    time_step: 3600
    nproc: 360
    omp_num_threads: 6
    add_namelist_changes:
        fort.4:
            NAMCLDP:
                SCLCT_SWITCH: 2
            NAMORB:
                LCORBMD: true
                ORBMODE: "fixed_parameters"
                ORBECCEN: 0.016764
                ORBOBLIQ: 23.459
                ORBMVELP: 100.33
            NAEPHY:
                RCIMIN: 0

oasis3mct:
    lresume: true # Set to false to generate the rst files for first leg
    time_step: 7200
    use_lucia: true

xios:
    with_model: oifs
    nproc: 2
    omp_num_threads: 48


