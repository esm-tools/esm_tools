import os
import sys

sys.path.append(os.path.join(os.path.dirname(__file__), "../helpers"))

# Note: The finished_config fixture is now defined in tests/conftest.py
# so it's available globally. This conftest.py is kept for fixture-specific
# tests and organization.
