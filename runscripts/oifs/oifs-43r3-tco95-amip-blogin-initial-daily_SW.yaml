general:
        setup_name: oifs
        compute_time: "00:30:00"
        account: shkifmsw
        initial_date: "2008-01-01"
        final_date: "2008-01-03"
        model_dir: /home/shkifmsw/esm/models/oifs-43r3-foci/
        base_dir: /home/shkifmsw/esm/esm-experiments/
        nyear: 0
        nmonth: 0
        nday: 1
        restart_rate: 1
        restart_unit: days 

oifs:
        version: 43r3
        pool_dir: /scratch/usr/shkifmsw/foci_input2
        input_dir: ${pool_dir}/OPENIFS43R3-TCO95/
        rtables_dir: ${pool_dir}/OPENIFS43R3_GENERAL/rtables/
        vtables_dir: ${pool_dir}/OPENIFS43R3_GENERAL/vtables/
        clim_dir: ${pool_dir}/OPENIFS43R3-TCO95/95_4/
        ifsdata_dir: ${pool_dir}/OPENIFS43R3_GENERAL/ifsdata/
        cmip5_data_dir: ${pool_dir}/OPENIFS40R1_GENERAL/cmip5_ghg/
        icmcl_dir: ${pool_dir}/OPENIFS43R3-TCO95/2008010100/
        icmcl_file: ICMCLhagwINIT
        prepifs_expid: hagw
        prepifs_startdate: 20080101
        resolution: TCO95
        levels: L91
        lresume: 0
        post_processing: 1
        nproca: 288
        nprocb: 1
        perturb: 0 

# esm-plugins is in an early stage and has many limitations:
# 1. plugins MUST be placed in ../../esm_software/esm_runscripts/esm_plugins.yaml
#    and commented unless available
# 2. esm_plugin_manager cannot resolve variables at the momenty
# 3. git-url is only for documentation purposes, if location does not 
#    exists, esm_runscripts stops with an (useful) error message
#plugins:
#        preprocess:
#                #location: ${model_dir} 
#                location: /home/shkifmsw/esm/esm-tools-plugins/preprocess 
#                git-url: https://github.com/esm-tools-plugins/preprocess.git
#                preprocess:
#                        - preprocess

# include plugins like this:
#plugins:
#        my_plugin:
#                location: "/work/ollie/dbarbi/my_plugin"
#                git-url: ""
#                my_plugin:
#                        - "write_something"

