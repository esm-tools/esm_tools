computer:
   # launcher_flags: -l --kill-on-bad-exit=1 --nodes=${SLURM_JOB_NUM_NODES:-1} --distribution=plane --hint=nomulti     thread --ntasks=$((no_of_nodes * mpi_procs_pernode)) --ntasks-per-node=${mpi_procs_pernode} --cpus-per-task=${OMP_NUM_THREADS}
   launcher_flags: -l --kill-on-bad-exit=1 --distribution=plane 
general:
   use_venv: false
   homedir: !ENV ${HOME}
   setup_name: icon
   compute_time: "01:45:00"
   account: bb1198
   initial_date: "1979-01-01T00:00:00"       # Initial exp. date
   final_date: "1981-01-01T00:00:00"         # Final date of the experiment
   nmonth: 12
   
   base_dir: ${homedir}/esm/esm-experiments/ 
   model_dir: ${homedir}/esm/models/icon-2.6.6

icon:
   version: 2.6.6
   nproc: 256
   omp_num_threads: 8
   
   modelTimeStep: PT4M
   radTimeStep: PT60M
   checkpoint_interval: P01Y
   restart_interval: P01Y
   output_interval: P01M
   namelist_changes:
      NAMELIST_atm:
         aes_phy_nml:
            aes_phy_config(1)%dt_rad: ${radTimeStep} 
            aes_phy_config(1)%dt_vdf: ${modelTimeStep} 
            aes_phy_config(1)%dt_mig: ${modelTimeStep}
