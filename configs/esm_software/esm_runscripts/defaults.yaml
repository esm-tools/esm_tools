general:
        openmp: False
        heterogeneous_parallelization: False

        request_nodes_instead_of_cores: False

        choose_heterogeneous_parallelization:
                true:
                        openmp: True
                        request_nodes_instead_of_cores: True
                false:
                        openmp: False


per_model_defaults:
        file_movements:
                default:
                        all_directions: copy
                bin:
                        init_to_exp:    copy
                        exp_to_run:     copy
                        run_to_work:    copy
                        work_to_run:    copy

