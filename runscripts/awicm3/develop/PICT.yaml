general:
    user: !ENV ${USER}
    setup_name: "awicm3"
    version: "develop"
    account: "ab0246"
    compute_time: "08:00:00"
    initial_date: "1850-01-01"
    final_date: "1870-01-01"
    use_venv: true
    install_esm_tools_branch: "feat/awiesm3-v3.4-co2"
    base_dir: "/work/bb1469/${user}/runtime/${general.setup_name}-${general.version}/"
    #base_dir: "/work/ab0995/${user}/runtime/${general.setup_name}-${general.version}/" 
    nday: 0
    nmonth: 0
    nyear: 2
    profile: True
    parallel_file_movements: 'threads'

computer:
    taskset: true

awicm3:
    postprocessing: false
    model_dir: "/work/ab0246/${user}/model_codes/${general.setup_name}-${general.version}//"
    #model_dir: "/work/ab0995/${user}/model_codes/${general.setup_name}-${general.version}//"

fesom:
    resolution: "CORE2"
    restart_rate: 2
    restart_unit: "y"
    restart_first: 2
    lresume: false
    time_step: 1800
    nproc: 768
    omp_num_threads: 4
    tar_binary_restarts: true
    ini_parent_exp_id: "SPIN"
    ini_parent_date: "${prev_date}"
    ini_restart_dir: "/work/bb1469/a270092/runtime/awicm3-develop/SPIN/restart/fesom"
    choose_general.run_number:
        1:
            restart_in_sources:
                par_oce_restart: /${ini_restart_dir}/fesom.1849.oce.restart/*.nc
                par_ice_restart: /${ini_restart_dir}/fesom.1849.ice.restart/*.nc

oifs:
    resolution: "TCO95"
    levels: "L91"
    prepifs_expid: ab45
    input_expid: awi3
    wam: true
    lresume: false
    time_step: 3600
    nproc: 384
    omp_num_threads: 6
    mip: 'cmip6'
    add_namelist_changes:
        fort.4:
            NAMECECMIP:
                NCMIPFIXYR: 1850
            NAERAD:
                NRADFR: -3
                NDECOLAT: 0
            NAMCLDP:
                RVICE: 0.19
            NAMGWWMS:
                GGAUSSB: -0.8

oasis3mct:
    use_lucia: True
    lresume: true # Set to false to generate the rst files for first leg
    time_step: 3600

xios:
    with_model: oifs
    nproc: 4
    omp_num_threads: 32

