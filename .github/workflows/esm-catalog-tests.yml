name: esm_catalog tests

on:
  push:
    paths:
      - "src/esm_catalog/**"
      - ".github/workflows/esm-catalog-tests.yml"
      - "setup.py"
  pull_request:
    paths:
      - "src/esm_catalog/**"
      - ".github/workflows/esm-catalog-tests.yml"
      - "setup.py"

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[catalog]"
          # Base setup.py pins pytest==7.1.2, which predates Python 3.12 support;
          # install a newer pytest explicitly for the catalog test job.
          pip install -U "pytest>=7.4"
      - name: Run esm_catalog tests
        run: pytest src/esm_catalog/tests -v
