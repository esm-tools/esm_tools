# ESM-Tests in check mode for all computers
# =========================================

name: esm_tests

# Triggers the workflow on a push or a pull
on: [pull_request]

jobs:
  ollie:
    uses: ./.github/workflows/esm_tests_computer_workflow.yml
    with:
        computer: ollie1
        update_esm_tests_info: $UPDATE_ESM_TESTS_INFO
  levante:
    uses: ./.github/workflows/esm_tests_computer_workflow.yml
    with:
        computer: levante1
        update_esm_tests_info: $UPDATE_ESM_TESTS_INFO
  juwels:
    uses: ./.github/workflows/esm_tests_computer_workflow.yml
    with:
        computer: jwlogin01.juwels
        update_esm_tests_info: $UPDATE_ESM_TESTS_INFO
  aleph:
    uses: ./.github/workflows/esm_tests_computer_workflow.yml
    with:
        computer: elogin1
        update_esm_tests_info: $UPDATE_ESM_TESTS_INFO
  blogin:
    uses: ./.github/workflows/esm_tests_computer_workflow.yml
    with:
        computer: blogin1
        update_esm_tests_info: $UPDATE_ESM_TESTS_INFO
